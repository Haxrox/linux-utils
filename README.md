# Ubuntu
Ubuntu dependencies

**Desktop:**
- `sudo ./install.sh`

**Grub2:**
- `sudo ./install.sh -v -4 -w`

**Icon:**
- `sudo ./install.sh`

**.bash_personal:**
```bash
cp .bash_personal ~
echo -e "\nsource ~/.bash_personal >> ~/.bashrc"
```
